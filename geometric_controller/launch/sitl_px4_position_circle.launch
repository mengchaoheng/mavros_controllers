<launch>
  <arg name="mav_name" default="iris"/>
  <arg name="fcu_url" default="udp://:14540@127.0.0.1:14557"/>
  <arg name="gcs_url" default="" />
  <arg name="tgt_system" default="1" />
  <arg name="tgt_component" default="1" />
  <arg name="gazebo_simulation" default="true" />
  <arg name="gazebo_gui" default="true"/>
  <arg name="visualization" default="false"/>
  <arg name="log_output" default="screen" />
  <arg name="fcu_protocol" default="v2.0" />
  <arg name="respawn_mavros" default="false" />

  <arg name="shapeOmega" default="0.5" />
  <arg name="takeoffHoverZ" default="2.0" />
  <arg name="startPositionTolerance" default="0.35" />
  <arg name="startVelocityTolerance" default="0.45" />
  <arg name="startHoldDuration" default="1.0" />

  <!-- Only switches PX4 into OFFBOARD and arms. It does not publish body-rate commands. -->
  <node pkg="geometric_controller" type="geometric_controller_node" name="offboard_manager" output="screen">
        <param name="mav_name" type="string" value="$(arg mav_name)" />
        <param name="enable_sim" value="$(arg gazebo_simulation)" />
        <param name="offboard_manager_only" value="true" />
  </node>

  <!-- Legacy circle trajectory, sent as position setpoints for PX4's built-in position controller. -->
  <node pkg="trajectory_publisher" type="trajectory_publisher" name="trajectory_publisher" output="screen">
        <param name="trajName" value="1" />
        <param name="shapeOmega" value="$(arg shapeOmega)" />
        <param name="initpos_z" value="$(arg takeoffHoverZ)" />
        <param name="reference_type" value="32" />
        <param name="waitStartBeforeTrajectory" value="true" />
        <param name="startPositionTolerance" value="$(arg startPositionTolerance)" />
        <param name="startVelocityTolerance" value="$(arg startVelocityTolerance)" />
        <param name="startHoldDuration" value="$(arg startHoldDuration)" />
        <param name="trajectoryStartRampDuration" value="2.0" />
  </node>

  <include file="$(find mavros)/launch/node.launch">
      <arg name="pluginlists_yaml" value="$(find mavros)/launch/px4_pluginlists.yaml" />
      <arg name="config_yaml" value="$(find mavros)/launch/px4_config.yaml" />
      <arg name="fcu_url" value="$(arg fcu_url)" />
      <arg name="gcs_url" value="$(arg gcs_url)" />
      <arg name="tgt_system" value="$(arg tgt_system)" />
      <arg name="tgt_component" value="$(arg tgt_component)" />
      <arg name="log_output" value="$(arg log_output)" />
      <arg name="fcu_protocol" value="$(arg fcu_protocol)" />
      <arg name="respawn_mavros" default="$(arg respawn_mavros)" />
  </include>

  <include file="$(find px4)/launch/posix_sitl.launch">
      <arg name="vehicle" value="$(arg mav_name)"/>
      <arg name="gui" value="$(arg gazebo_gui)"/>
  </include>

  <group if="$(arg visualization)">
      <node type="rviz" name="rviz" pkg="rviz" args="-d $(find geometric_controller)/launch/config_file.rviz" />
  </group>
</launch>
